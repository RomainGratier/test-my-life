# This file contains knowledge or configuration specific to Zoey.
# It has been moved to the correct location as requested.

# Example content (replace with actual knowledge data as needed):
version: 1.0
settings:
  feature_enabled: true
  log_level: info
data:
  - item: "task_management"
    description: "Handles task creation, assignment, and tracking."
  - item: "user_preferences"
    description: "Stores user-specific settings and display options."